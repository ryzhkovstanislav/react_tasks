import React from 'react';

const footerStyle = {
    background: '#424242',
    height: '150px',
};

const Footer = () => {
    return(
        <footer style={footerStyle}>
        </footer>
    )
};

export default Footer;
